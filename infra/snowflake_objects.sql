create or replace database DEMO_AI_PLATFORM;
create or replace schema DEMO_AI_PLATFORM.RAW;
create or replace schema DEMO_AI_PLATFORM.CURATED;
create or replace schema DEMO_AI_PLATFORM.FEATURES;

create or replace stage DEMO_AI_PLATFORM.RAW.INGEST_STAGE;

-- Replace with pipes, streams, tasks, and role grants for your environment.
